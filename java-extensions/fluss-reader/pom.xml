<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.starrocks</groupId>
        <artifactId>java-extensions</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>fluss-reader</artifactId>

    <properties>
        <java-extensions.home>${basedir}/../</java-extensions.home>
        <fluss.version>0.9.1-incubating</fluss.version>
        <paimon.version>1.3.1</paimon.version>
        <!-- NOTE: iceberg.version is deliberately NOT overridden here. It is inherited from
             java-extensions/pom.xml (1.10.0), which matches fe/pom.xml (1.10.0). Fluss 0.9.1
             itself builds against 1.10.1, but the FE serializes the IcebergSplit (including
             org.apache.iceberg.PartitionData) and the BE reader deserializes it via plain Java
             serialization, so any version difference across that boundary is fatal:
               InvalidClassException: org.apache.iceberg.PartitionData; local class incompatible:
               stream serialVersionUID = 5861549261575484086, local = -2455556416558591026
             PartitionData's serialVersionUID changed between 1.10.0 and 1.10.1, so FE and BE
             MUST agree. "Patch-level skew is harmless" does not hold once Java serialization
             crosses the FE/BE boundary. -->
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.starrocks</groupId>
            <artifactId>jni-connector</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-hdfs-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-mapreduce-client-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>4.11.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.apache.fluss</groupId>
            <artifactId>fluss-client</artifactId>
            <version>${fluss.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.fluss</groupId>
            <artifactId>fluss-common</artifactId>
            <version>${fluss.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.fluss</groupId>
            <artifactId>fluss-lake-paimon</artifactId>
            <version>${fluss.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.fluss</groupId>
            <artifactId>fluss-lake-iceberg</artifactId>
            <version>${fluss.version}</version>
        </dependency>

        <!-- Iceberg pinned to the inherited ${iceberg.version} (1.10.0), matching fe/pom.xml.
             This ALONE is not sufficient: see strip-shaded-iceberg.py in fluss-local/. The
             shaded fluss-lake-iceberg jar must also have its unrelocated org/apache/iceberg
             classes removed, otherwise the BE resolves PartitionData from the shaded 1.10.1 copy
             while the FE resolves it from iceberg-core-1.10.0, and the split handoff fails with
             InvalidClassException on mismatched serialVersionUID. -->
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-core</artifactId>
            <version>${iceberg.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-api</artifactId>
            <version>${iceberg.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-common</artifactId>
            <version>${iceberg.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-bundled-guava</artifactId>
            <version>${iceberg.version}</version>
        </dependency>

        <!-- Historical note on why the explicit deps above are required.
             fluss-lake-iceberg-0.9.1-incubating.jar is a 36MB SHADED uber-jar carrying ~6270
             org/apache/iceberg/* classes UNRELOCATED, including org.apache.iceberg.PartitionData
             (built against Iceberg 1.10.1). Adding iceberg-core et al. puts a SECOND, different
             PartitionData on the classpath, and whichever wins depends on classpath order. FE and
             BE then resolve different copies and the split handoff dies with:
               InvalidClassException: org.apache.iceberg.PartitionData; local class incompatible:
               stream serialVersionUID = 5861549261575484086, local = -2455556416558591026
             Relying solely on the bundled copy keeps a single definition in the reader.
             NOTE FOR UPSTREAM: an unrelocated shaded jar is a packaging bug in fluss-lake-iceberg;
             it collides with any host that already ships Iceberg (StarRocks ships 1.10.0 for its
             own iceberg catalog). Worth filing against Fluss. -->

        <dependency>
            <groupId>org.apache.fluss</groupId>
            <artifactId>fluss-flink-common</artifactId>
            <version>${fluss.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.flink</groupId>
            <artifactId>flink-core</artifactId>
            <version>1.20.1</version>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-bundle</artifactId>
            <version>${paimon.version}</version>
            <exclusions>
                <exclusion>
                    <artifactId>lz4-java</artifactId>
                    <groupId>org.lz4</groupId>
                </exclusion>
                <exclusion>
                    <groupId>com.google.protobuf</groupId>
                    <artifactId>protobuf-java</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.httpcomponents.client5</groupId>
                    <artifactId>httpclient5</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-oss</artifactId>
            <version>${paimon.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.paimon</groupId>
            <artifactId>paimon-s3</artifactId>
            <version>${paimon.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>starrocks-fluss-reader</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.1.1</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/fluss-reader-lib</outputDirectory>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
